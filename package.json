{
  "name": "thread",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Thread — a free browser precision skill game.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "build:itch": "tsc --noEmit && VITE_BASE=./ vite build --base ./ && node scripts/pack-itch.mjs",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vite": "^7.1.5",
    "vitest": "^3.2.4"
  }
}
